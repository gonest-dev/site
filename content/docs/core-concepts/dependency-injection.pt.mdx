---
title: Injeção de Dependência
description: MustInject, MustInjectAll e multi-binding
---

O gonest resolve dependências pelo **tipo**, não por nome ou token de string.

## `MustInject[T](owner)`

Resolve exatamente uma instância de `T`. Dá panic se zero ou mais de uma correspondência for
encontrada.

- **Tipos de ponteiro** (`*UserService`) exigem uma correspondência exata de tipo.
- **Tipos de interface** também exigem uma correspondência exata — não existe mais uma alternativa
  estrutural. Um Provider só se torna resolvível por uma interface `T` se ele foi explicitamente
  embrulhado (wrapped) pra `T` via `gonest.ProviderAs[T](ref)`; veja
  [Exportando um Provider como uma interface](/docs/core-concepts/providers#exportando-um-provider-como-uma-interface).

```go
userService := gonest.MustInject[*UserService](controller)
```

## `MustInjectAll[T](owner)`

Resolve **todos** os Providers que correspondem a `T`. Nunca dá panic em 0 ou N correspondências — use-o
para padrões de plugin/estratégia (plugin/strategy patterns) onde vários Providers validamente
implementam a mesma interface.

```go
type Pingable interface {
  Name() string
  Ping(ctx context.Context) error
}

var HealthController_ = gonest.NewController(func(controller *gonest.Controller) {
  controller.Path("/health")
  pingables := gonest.MustInjectAll[Pingable](controller)
  _ = pingables
})
```

<Callout type="info">
  Esse é o mesmo padrão usado para [Health Checks](/docs/health-checks): qualquer
  número de serviços pode implementar `Pingable`, e o Controller de saúde descobre todos eles automaticamente.
</Callout>

## Onde `MustInject` pode ser chamado

`MustInject`/`MustInjectAll` são legais dentro de construtores de `Provider`, `Controller`
— em qualquer lugar que o framework garanta que a Fase 1 (resolução de Providers) já
foi concluída. Veja [Bootstrap](/docs/getting-started/bootstrap).

A partir de um owner `Provider`, `MustInjectAll[T](owner)` resolve de forma diferente
de um owner `Controller`/`Middleware`/`Guard`/`Interceptor`/`Filter`. O construtor
(builder fn) de um `Provider` roda antes de qualquer instância existir, então, em vez de
resolver imediatamente, `MustInjectAll` já devolve um slice de comprimento fixo — já
dimensionado pra quantos Providers no escopo correspondem a `T` — e cada elemento é
preenchido in-place assim que o Provider correspondente de fato resolve. Só Providers
`gonest.ScopeSingleton` podem ser membros desse slice: um Provider `ScopeTransient` que
corresponderia faz `NewApp`/`MustNewApp` dar panic. A ordem dos elementos do slice não é
garantida, e zero correspondências ainda retorna um slice vazio em vez de panic — o mesmo
contrato do caminho de resolução direta acima.

Os construtores de `Guard` são a única exceção: eles **não** têm suporte a `MustInject`,
porque um único Guard pode ser anexado em vários Controllers/Modules —
não há um único "proprietário" (owner) para resolver em relação a ele.

## Builder fn, não `Constructor`

`MustInject`/`MustInjectAll` precisam ser chamados no **builder fn** — a função passada pra
`gonest.NewProvider`/`NewController`/etc — **antes** de `Constructor(...)` ser registrado, nunca
de dentro do próprio closure de `Constructor`:

```go
var UserService_ = gonest.NewProvider(func(p *gonest.Provider) {
  // ✅ correto: resolvido aqui, no builder fn
  repo := gonest.MustInject[*UserRepository](p)

  p.Constructor(func() *UserService {
    // ❌ errado: chamando MustInject/MustInjectAll DENTRO de Constructor
    // repo := gonest.MustInject[*UserRepository](p)
    return &UserService{repo: repo}
  })
})
```

O builder fn de um `Provider` roda durante a Fase 2 (Declare), antes da Fase 3 montar o grafo
de dependências ou invocar um único `Constructor`. Chamar corretamente ali deixa a Fase 3
resolver a dependência e preenchê-la antes de `Constructor` sequer rodar. Chamar de dentro de
`Constructor` significa que a Fase 3 já está rodando — o grafo de dependências já foi montado,
toda goroutine já foi despachada — então a chamada chega tarde demais pra qualquer coisa
consultar. `NewApp`/`New` agora dá panic com uma mensagem nomeando o tipo exato (`gonest:
MustInject[T] called from inside a Constructor — ...`) se isso acontecer, então o engano
aparece imediatamente no bootstrap em vez de deixar uma dependência permanentemente zerada sem
nenhum erro em lugar nenhum.

## Próximos passos

<Cards>
  <Card title="Request Pipeline" href="/docs/request-pipeline" />
</Cards>
