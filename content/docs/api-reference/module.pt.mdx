---
title: Module
description: Referência de gonest.NewModule
---

`gonest.NewModule(func(module *gonest.Module) {...})`

| Método | Descrição |
|---|---|
| `Providers(...)` | Registra Providers de propriedade deste Module. |
| `Controllers(...)` | Registra Controllers de propriedade deste Module. |
| `Imports(...)` | Importa outros Modules, obtendo acesso aos seus Providers exportados. |
| `Exports(...)` | Expõe Providers para Modules que importam este. |
| `Lazy(func(l *gonest.LazyModule) {...})` | Decide, de forma síncrona e de DENTRO do grafo de DI, qual(is) Module(s) `Imports`/`Exports` — a partir de um valor de config lido via `gonest.MustInject[T](l)` dos próprios `Providers(...)` deste Module. Equivalente do gonest ao `DynamicModule.forRootAsync` do NestJS. |
| `Use(...)` | Registra Middleware global. Apenas no Module raiz. |
| `Filters(...)` | Registra Filters de exceção globais. Apenas no Module raiz. |
| `Listeners(...)` | Registra ouvintes (Listeners) de eventos. |
| `Schedulers(...)` | Registra tarefas (jobs) do Scheduler. |
| `Name("...")` | Define um nome legível usado em mensagens de erro. |

Veja: [Modules](/docs/core-concepts/modules)
