---
title: Middleware
description: Referência de gonest.NewMiddleware
---

`gonest.NewMiddleware(func(middleware *gonest.Middleware) {...})`

| Método | Descrição |
|---|---|
| `Handler(func(c *gonest.HttpContext, next gonest.Next))` | `next(c)` continua a cadeia; omiti-lo causa curto-circuito (short-circuit). |

Aplicado via `Module.Use(...)` (global, apenas no raiz) ou `Controller.Use(...)` (escopo do Controller).

Veja: [Middleware](/docs/request-pipeline/middleware)
